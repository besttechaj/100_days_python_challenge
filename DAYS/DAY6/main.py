# some built-in functions in pythons are: print, len
print('hello')
temp = len('what is going on ?')
print(temp)

### Functions:
# Whenever you want to perform some particular task is known as function.
# To use the function?? First we have define the function and then have to call it using its name.
# We can execute a single function n number of times.

# defining function
def my_function():
  ## the code must be intended inside the block ie., the code should not start from the outside the loop, it must be intended inside. 
  print('this is custom function')
  print('function ends here')

# calling function
my_function()